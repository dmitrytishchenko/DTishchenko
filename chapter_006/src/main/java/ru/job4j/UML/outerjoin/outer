--create table bodycar(id serial primary key,name varchar(100));
--create table engine(id serial primary key, name varchar(100));
--create table transmission(id serial primary key,name varchar(100));
--select *from bodycar;
--select *from engine;
--select *from transmission;
--insert into bodycar(name) values('унивесал');
--insert into bodycar(name) values('хэтчбэк');
--insert into bodycar(name) values('седан');
--insert into bodycar(name) values('кабриолет');
--insert into engine(name) values('бензиновый');
--insert into engine(name) values('дизельный');
--insert into engine(name) values('электрический');
--insert into transmission(name) values('автомат'); 
--insert into transmission(name) values('робот'); 
--insert into transmission(name) values('механика'); 
--insert into transmission(name) values('вариатор'); 
--create table cars(id serial primary key, name varchar(100), 
--id_bodycar int references  bodycar(id),
-- id_engine int references  engine(id),
-- id_transmission int references  transmission(id));

--insert into cars(name, id_bodycar, id_engine, id_transmission) values('Lada', 3, 1, 3);
--insert into cars(name, id_bodycar, id_engine, id_transmission) values('Kia', 1, 2, 1);
--insert into cars(name, id_bodycar, id_engine, id_transmission) values('Scoda', 2, 1, 2);
--select*from cars;

--select c.name, b.name, e.name, t.name from cars as c
--left outer join bodycar as b on c.id_bodycar=b.id 
--left outer join engine as e on c.id_engine=e.id
--left outer join transmission as t on c.id_transmission= t.id;

--select c.name, b.name, e.name, t.name from cars as c
--right outer join bodycar as b on c.id_bodycar=b.id
--right outer join engine as e on c.id_engine=e.id
--right outer join transmission as t on c.id_transmission=t.id 
--where b.id is null or e.id is null or t.id is null;
